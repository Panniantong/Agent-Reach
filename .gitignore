__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/
.env
.agent-reach/
*.log

# No workflow consumes a uv lockfile (build=hatchling, CI=pip); keep it out
# until the project actually adopts `uv sync --locked`
uv.lock

# Claude Code personal permission settings — local only, never commit
.claude/settings.local.json
