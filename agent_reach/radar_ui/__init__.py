# -*- coding: utf-8 -*-
"""Web console for the radar (optional extra: ``pip install "agent-reach[ui]"``).

FastAPI server bound to 127.0.0.1 only + a single-file dark-terminal UI.
Heavy imports (fastapi/uvicorn) stay inside functions so the package is
importable without the extra.
"""
